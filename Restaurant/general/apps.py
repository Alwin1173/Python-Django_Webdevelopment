# No need to edit
from django.apps import AppConfig


class GeneralConfig(AppConfig):
    # if edited check the below ones
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'general'
